'use client'

import ScrollRevealTitle from '@/components/ScrollRevealTitle'

const LABEL = '/ RECENT WORK /'

export default function AboutMeTransition() {
  return <ScrollRevealTitle label={LABEL} />
}
